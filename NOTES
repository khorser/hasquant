Rethink exceptions strategy

Think of high-level combinators from data types like

class Instrument
data Bond = Bond ... | FixedRateBond ... | FloatingRateBond ...
instance Instrument Bond

data PricingEngine = Engine1 ... | Engine2 ...

data Result = Result{npv :: Double, ...}
calculate :: Instrument -> PricingEngine -> IO Result

Abstract data types for Currency, Calendar...

While building components for pricing, we will construct contents.
Actual instantiation of QuantLib object will happen only on pricing.
This allows for easy building of mutators as simply returning a new copy with
updated data

try and make objects as much immutable as possible by not exposing mutators
(except maybe Calendars that should keep the ability to be changed if the user wants
to introduce adjustments)
as result that Handle/RelinkableHandle machinery isn't going to be much of help

DSL for periods: create, arithmetic, arithmetic of periods and dates

If needed, we could use FRP to model the propagation of changes in market data

Introduce money data type parameterized by currency

Implement `show' via operator <<

sessions?

review use of IO in functions and remove it from those not raising exceptions and not depending
on the state

Plan: migrate business calculation first and then think of reusing the underlying math

Coupons; Monte Carlo; Trees, Latices, Discretized

name conflicts between methods of unrelated classes

No empty objects for ccy, calendar, day counter!

Custom DayCounters calling back to Haskell, many foreign imports would become unsafe then?

Expose as less mutators as possible

instead of CompositeQuote/DerivedQuote implement a quote calling Haskell callbacks if needed.
Probably it would need to register for notifications from other quotes and evaluation date?

(Maybe ...) in too many functions, look for Maybe Day (qlNullableDate) and qlNullableHandle?

add Interpolation2D

String allocations/deallocations and catching of exceptions can happen only in IO so most C FFI
functions live in IO

many bond functions have IO simply because floating bonds use YieldTermStructure
(and consequently floating coupons)

use type magic to ensure Day is always valid QuantLib date

scary error messages when some header is not included

idea: custom name handling to check types of C functions with their FFI desc
generate information about casting hierarchy

qlaux.h contains common infrastructure (allocation/deallocation, typedefs, memory tracking etc) 
*Aux.cpp files contain some template-heavy code. They do not depend on qlaux.h so they are not
getting rebuilt when qlaux.h changes

Allocate named objects without attaching finalizers to them. This way we can live without IO
for Calendar/DayCounter/Currency. And then we can define Read instance for them

-- workflow for code generation:
generate doxygen docs (you can find the copy in `doxygen` dir)
convert to txt (see `doxygen-txt`)
split txt (see `doxygen-split`)
error handling is minimal, read comments in the scripts before running

