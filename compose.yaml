# The image's dev user must match the host UID/GID that owns the bind-mounted
# working tree, or it can't write .stack-work/dist-newstyle. bash's $UID is
# read-only and not exported by default, so run e.g.
#   UID=$(id -u) GID=$(id -g) docker compose build
# or export UID/GID in your shell profile.
services:
  hasquant:
    build:
      context: .
    # target: development
      args:
        BOOST_VERSION: "1.91.0-1"
        QL_VERSION: "1.43"
        GHC_VERSION: "9.10.3"
        UID: "${UID:-1000}"
        GID: "${GID:-1000}"

    image: hasquant:default
    user: "dev"
    platform: linux/amd64
    init: true
    working_dir: /hasquant
    container_name: hasquant-dev

    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined

    volumes:
      - .:/hasquant:delegated
      - stack-root:/home/dev/.stack
      - ghcup-root:/home/dev/.ghcup
      - cabal-root:/home/dev/.cabal
      - /hasquant/.stack-work
      - /hasquant/dist-newstyle
    stdin_open: true
    tty: true
    #environment:
    #  - TERM=dumb
    #  - STACK_IN_CONTAINER=true

volumes:
  stack-root:
    name: stack-root
  ghcup-root:
    name: ghcup-root
  cabal-root:
    name: cabal-root
