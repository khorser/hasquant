IMM
 -- |Main cycle of the International Money Market (a.k.a. IMM) months.

 -e-
  Month
   F
   G
   H
   J
   K
   M
   N
   Q
   U
   V
   X
   Z
 -f-
  -- |returns whether or not the given date is an IMM date
  isIMMdate
   ->bool
   ::const Date &
   -- ^d
   ::bool
   =true
   -- ^mainCycle
  -- |returns whether or not the given string is an IMM code
  isIMMcode
   ->bool
   ::const std::string &
   -- ^in
   ::bool
   =true
   -- ^mainCycle
  -- |returns the IMM code for the given date (e.g. H3 for March 20th, 2013).WarningIt raises an exception if the input date is not an IMM date
  code
   ->std::string
   ::const Date &
   -- ^immDate
  -- |returns the IMM date for the given IMM code (e.g. March 20th, 2013 for H3).WarningIt raises an exception if the input string is not an IMM code
  date
   ->Date
   ::const std::string &
   -- ^immCode
   ::const Date &
   =Date()
   -- ^referenceDate
  -- |next IMM date following the given date
  -- |returns the 1st delivery date for next contract listed in the International Money Market section of the Chicago Mercantile Exchange.
  nextDate
   ->Date
   ::const Date &
   =Date()
   -- ^d
   ::bool
   =true
   -- ^mainCycle
  -- |next IMM date following the given IMM code
  -- |returns the 1st delivery date for next contract listed in the International Money Market section of the Chicago Mercantile Exchange.
  nextDate
   ->Date
   ::const std::string &
   -- ^immCode
   ::bool
   =true
   -- ^mainCycle
   ::const Date &
   =Date()
   -- ^referenceDate
  -- |next IMM code following the given date
  -- |returns the IMM code for next contract listed in the International Money Market section of the Chicago Mercantile Exchange.
  nextCode
   ->std::string
   ::const Date &
   =Date()
   -- ^d
   ::bool
   =true
   -- ^mainCycle
  -- |next IMM code following the given code
  -- |returns the IMM code for next contract listed in the International Money Market section of the Chicago Mercantile Exchange.
  nextCode
   ->std::string
   ::const std::string &
   -- ^immCode
   ::bool
   =true
   -- ^mainCycle
   ::const Date &
   =Date()
   -- ^referenceDate