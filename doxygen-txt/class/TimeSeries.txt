TimeSeries
 -- |Container for historical data.
 -- |This class acts as a generic repository for a set of historical data. Any single datum can be accessed through its date, while sets of consecutive data can be accessed through iterators.The Container type must satisfy the requirements set by the C++ standard for associative containers.
 -f-
  -- |returns the first date for which a historical datum exists
  firstDate
   ->Date
  -- |returns the last date for which a historical datum exists
  lastDate
   ->Date
  -- |returns the number of historical data including null ones
  size
   ->Size
  -- |returns whether the series contains any data
  empty
   ->bool
 -f-
  -- |returns the (possibly null) datum corresponding to the given date
  operator[]
   ->T
   ::const Date &
   -- ^d
  operator[]
   ->T &
   ::const Date &
   -- ^d
 -f-
  cbegin
   ->const_iterator
  cend
   ->const_iterator
  begin
   ->const_iterator
  end
   ->const_iterator
  crbegin
   ->const_reverse_iterator
  crend
   ->const_reverse_iterator
  rbegin
   ->const_reverse_iterator
  rend
   ->const_reverse_iterator
 -f-
  cbegin_values
   ->const_value_iterator
  cend_values
   ->const_value_iterator
  crbegin_values
   ->const_reverse_value_iterator
  crend_values
   ->const_reverse_value_iterator
  cbegin_time
   ->const_time_iterator
  cend_time
   ->const_time_iterator
  crbegin_time
   ->const_reverse_time_iterator
  crend_time
   ->const_reverse_time_iterator
 -f-
  find
   ->const_iterator
   ::const Date &
   -- ^
  -- |returns the dates for which historical data exist
  dates
   ->std::vector< Date >
  -- |returns the historical data
  values
   ->std::vector< T >
 -f-
  -- |Default constructor
  TimeSeries
   ->
  -- |This constructor initializes the history with a set of values passed as two sequences, the first containing dates and the second containing corresponding values.
  TimeSeries
   ->
   ::DateIterator
   -- ^dBegin
   ::DateIterator
   -- ^dEnd
   ::ValueIterator
   -- ^vBegin
  -- |This constructor initializes the history with a set of values. Such values are assigned to a corresponding number of consecutive dates starting from firstDate included.
  TimeSeries
   ->
   ::const Date &
   -- ^firstDate
   ::ValueIterator
   -- ^begin
   ::ValueIterator
   -- ^end
