Matrix
 -- |Matrix used in linear algebra.
 -- |This class implements the concept of Matrix as used in linear algebra. As such, it is not meant to be used as a container.

 -f-
  -- |creates a null matrix
  Matrix
   ->
  -- |creates a matrix with the given dimensions
  Matrix
   ->
   ::Size
   -- ^rows
   ::Size
   -- ^columns
  -- |creates the matrix and fills it with value
  Matrix
   ->
   ::Size
   -- ^rows
   ::Size
   -- ^columns
   ::Real
   -- ^value
  Matrix
   ->
   ::const Matrix &
   -- ^
  Matrix
   ->
   ::const Disposable< Matrix > &
   -- ^
  operator=
   ->Matrix &
   ::const Matrix &
   -- ^
  operator=
   ->Matrix &
   ::const Disposable< Matrix > &
   -- ^
 -f-
  -- |all matrices involved in an algebraic expression must have the same size.
  operator+=
   ->const Matrix &
   ::const Matrix &
   -- ^
  operator-=
   ->const Matrix &
   ::const Matrix &
   -- ^
  operator*=
   ->const Matrix &
   ::Real
   -- ^
  operator/=
   ->const Matrix &
   ::Real
   -- ^
 -f-
  begin
   ->const_iterator
  begin
   ->iterator
  end
   ->const_iterator
  end
   ->iterator
  rbegin
   ->const_reverse_iterator
  rbegin
   ->reverse_iterator
  rend
   ->const_reverse_iterator
  rend
   ->reverse_iterator
  row_begin
   ->const_row_iterator
   ::Size
   -- ^i
  row_begin
   ->row_iterator
   ::Size
   -- ^i
  row_end
   ->const_row_iterator
   ::Size
   -- ^i
  row_end
   ->row_iterator
   ::Size
   -- ^i
  row_rbegin
   ->const_reverse_row_iterator
   ::Size
   -- ^i
  row_rbegin
   ->reverse_row_iterator
   ::Size
   -- ^i
  row_rend
   ->const_reverse_row_iterator
   ::Size
   -- ^i
  row_rend
   ->reverse_row_iterator
   ::Size
   -- ^i
  column_begin
   ->const_column_iterator
   ::Size
   -- ^i
  column_begin
   ->column_iterator
   ::Size
   -- ^i
  column_end
   ->const_column_iterator
   ::Size
   -- ^i
  column_end
   ->column_iterator
   ::Size
   -- ^i
  column_rbegin
   ->const_reverse_column_iterator
   ::Size
   -- ^i
  column_rbegin
   ->reverse_column_iterator
   ::Size
   -- ^i
  column_rend
   ->const_reverse_column_iterator
   ::Size
   -- ^i
  column_rend
   ->reverse_column_iterator
   ::Size
   -- ^i
 -f-
  operator[]
   ->const_row_iterator
   ::Size
   -- ^
  at
   ->const_row_iterator
   ::Size
   -- ^
  operator[]
   ->row_iterator
   ::Size
   -- ^
  at
   ->row_iterator
   ::Size
   -- ^
  diagonal
   ->Disposable< Array >
   ::void
   -- ^
 -f-
  rows
   ->Size
  columns
   ->Size
  empty
   ->bool
 -f-
  swap
   ->void
   ::Matrix &
   -- ^
 -f-
  operator+
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
   ::const Matrix &
   -- ^
  operator-
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
   ::const Matrix &
   -- ^
  operator*
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
   ::Real
   -- ^
  operator*
   ->const Disposable< Matrix >
   ::Real
   -- ^
   ::const Matrix &
   -- ^
  operator/
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
   ::Real
   -- ^
  operator*
   ->const Disposable< Array >
   ::const Array &
   -- ^
   ::const Matrix &
   -- ^
  operator*
   ->const Disposable< Array >
   ::const Matrix &
   -- ^
   ::const Array &
   -- ^
  operator*
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
   ::const Matrix &
   -- ^
  transpose
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
  outerProduct
   ->const Disposable< Matrix >
   ::const Array &
   -- ^v1
   ::const Array &
   -- ^v2
  outerProduct
   ->const Disposable< Matrix >
   ::Iterator1
   -- ^v1begin
   ::Iterator1
   -- ^v1end
   ::Iterator2
   -- ^v2begin
   ::Iterator2
   -- ^v2end
  swap
   ->void
   ::Matrix &
   -- ^
   ::Matrix &
   -- ^
  operator<<
   ->std::ostream &
   ::std::ostream &
   -- ^
   ::const Matrix &
   -- ^
  inverse
   ->Disposable< Matrix >
   ::const Matrix &
   -- ^m
  determinant
   ->Real
   ::const Matrix &
   -- ^m
  CholeskyDecomposition
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^m
   ::bool
   =false
   -- ^flexible
  -- |Returns the pseudo square root of a real symmetric matrix.
  -- |Given a matrix $ M $, the result $ S $ is defined as the matrix such that $ S S^T = M. $ If the matrix is not positive semi definite, it can return an approximation of the pseudo square root using a (user selected) salvaging algorithm.For more information see: R. Rebonato and P. Jäckel, The most general methodology to create a valid correlation matrix for risk management and option pricing purposes, The Journal of Risk, 2(2), Winter 1999/2000. http://www.rebonato.com/correlationmatrix.pdfRevised and extended in "Monte Carlo Methods in Finance", by Peter Jäckel, Chapter 6.the given matrix must be symmetric. WarningHigham algorithm only works for correlation matrices.Tests the correctness of the results is tested by reproducing known good data.the correctness of the results is tested by checking returned values against numerical calculations.
  pseudoSqrt
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
   ::SalvagingAlgorithm::Type
   =SalvagingAlgorithm::None
   -- ^
  -- |Returns the rank-reduced pseudo square root of a real symmetric matrix.
  -- |The result matrix has rank<=maxRank. If maxRank>=size, then the specified percentage of eigenvalues out of the eigenvalues' sum is retained.If the input matrix is not positive semi definite, it can return an approximation of the pseudo square root using a (user selected) salvaging algorithm.the given matrix must be symmetric.
  rankReducedSqrt
   ->const Disposable< Matrix >
   ::const Matrix &
   -- ^
   ::Size
   -- ^maxRank
   ::Real
   -- ^componentRetainedPercentage
   ::SalvagingAlgorithm::Type
   -- ^