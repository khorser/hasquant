G2
 -- |Two-additive-factor gaussian model class.
 -- |This class implements a two-additive-factor model defined by \[ dr_t = \varphi(t) + x_t + y_t \] where $ x_t $ and $ y_t $ are defined by \[ dx_t = -a x_t dt + \sigma dW^1_t, x_0 = 0 \] \[ dy_t = -b y_t dt + \sigma dW^2_t, y_0 = 0 \] and $ dW^1_t dW^2_t = \rho dt $.BugThis class was not tested enough to guarantee its functionality.

 -f-
  G2
   ->
   ::const Handle< YieldTermStructure > &
   -- ^termStructure
   ::Real
   =0.1
   -- ^a
   ::Real
   =0.01
   -- ^sigma
   ::Real
   =0.1
   -- ^b
   ::Real
   =0.01
   -- ^eta
   ::Real
   =-0.75
   -- ^rho
  discountBond
   ->Real
   ::Time
   -- ^
   ::Time
   -- ^
   ::Rate
   -- ^
   ::Rate
   -- ^
  swaption
   ->Real
   ::const Swaption::arguments &
   -- ^arguments
   ::Rate
   -- ^fixedRate
   ::Real
   -- ^range
   ::Size
   -- ^intervals