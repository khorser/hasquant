CompositeQuote
 -- |market element whose value depends on two other market element
 -- |Teststhe correctness of the returned values is tested by checking them against numerical calculations.

 -f-
  value1
   ->Real
  value2
   ->Real
 -f-
  CompositeQuote
   ->
   ::const Handle< Quote > &
   -- ^element1
   ::const Handle< Quote > &
   -- ^element2
   ::const BinaryFunction &
   -- ^f