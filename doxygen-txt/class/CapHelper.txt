CapHelper
 -- |calibration helper for ATM cap
 -- |BugThis helper does not register with the passed IBOR index and with the evaluation date. Furthermore, the ATM strike rate is not recalculated when any of its observables change.

 -f-
  CapHelper
   ->
   ::const Period &
   -- ^length
   ::const Handle< Quote > &
   -- ^volatility
   ::const boost::shared_ptr< IborIndex > &
   -- ^index
   ::Frequency
   -- ^fixedLegFrequency
   ::const DayCounter &
   -- ^fixedLegDayCounter
   ::bool
   -- ^includeFirstSwaplet
   ::const Handle< YieldTermStructure > &
   -- ^termStructure
   ::CalibrationHelper::CalibrationErrorType
   =CalibrationHelper::RelativePriceError
   -- ^errorType