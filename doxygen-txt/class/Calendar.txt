Calendar

 -f-
  -- |Returns whether or not the calendar is initialized.
  empty
   ->bool
  -- |Returns the name of the calendar.
  -- |WarningThis method is used for output and comparison between calendars. It is not meant to be used for writing switch-on-type code.
  name
   ->std::string
  -- |Returns true iff the date is a business day for the given market.
  isBusinessDay
   ->bool
   ::const Date &
   -- ^d
  -- |Returns true iff the date is a holiday for the given market.
  isHoliday
   ->bool
   ::const Date &
   -- ^d
  -- |Returns true iff the weekday is part of the weekend for the given market.
  isWeekend
   ->bool
   ::Weekday
   -- ^w
  -- |Returns true iff the date is last business day for the month in given market.
  isEndOfMonth
   ->bool
   ::const Date &
   -- ^d
  -- |last business day of the month to which the given date belongs
  endOfMonth
   ->Date
   ::const Date &
   -- ^d
  -- |Adds a date to the set of holidays for the given calendar.
  addHoliday
   ->void
   ::const Date &
   -- ^
  -- |Removes a date from the set of holidays for the given calendar.
  removeHoliday
   ->void
   ::const Date &
   -- ^
  -- |Advances the given date as specified by the given period and returns the result. The input date is not modified.
  advance
   ->Date
   ::const Date &
   -- ^date
   ::const Period &
   -- ^period
   ::BusinessDayConvention
   =Following
   -- ^convention
   ::bool
   =false
   -- ^endOfMonth
  -- |Calculates the number of business days between two given dates and returns the result.
  businessDaysBetween
   ->BigInteger
   ::const Date &
   -- ^from
   ::const Date &
   -- ^to
   ::bool
   =true
   -- ^includeFirst
   ::bool
   =false
   -- ^includeLast
  -- |Returns the holidays between two dates.
  holidayList
   ->std::vector< Date >
   ::const Calendar &
   -- ^calendar
   ::const Date &
   -- ^from
   ::const Date &
   -- ^to
   ::bool
   =false
   -- ^includeWeekEnds
 -f-
  -- |The default constructor returns a calendar with a null implementation, which is therefore unusable except as a placeholder.
  Calendar
   ->
 -f-
  -- |Returns true iff the two calendars belong to the same derived class.
  operator==
   ->bool
   ::const Calendar &
   -- ^
   ::const Calendar &
   -- ^
  operator!=
   ->bool
   ::const Calendar &
   -- ^
   ::const Calendar &
   -- ^
  operator<<
   ->std::ostream &
   ::std::ostream &
   -- ^
   ::const Calendar &
   -- ^
