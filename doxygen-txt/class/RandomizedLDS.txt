RandomizedLDS
 -- |Randomized (random shift) low-discrepancy sequence.
 -- |Random-shifts a uniform low-discrepancy sequence of dimension $ N $ by adding (modulo 1 for each coordinate) a pseudo-random uniform deviate in $ (0, 1)^N. $ It is used for implementing Randomized Quasi Monte Carlo.The uniform low discrepancy sequence is supplied by LDS; the uniform pseudo-random sequence is supplied by PRS.Both class LDS and PRS must implement the following interface: LDS::sample_typeLDS::nextSequence()const; SizeLDS::dimension()const; LDS and PRS must have the same dimension $ N $ WarningInverting LDS and PRS is possible, but it doesn't make sense.Possible enhancementsimplement the other randomization algorithmsTestscorrect initialization is tested.
 -f-
  RandomizedLDS
   ->
   ::const LDS &
   -- ^ldsg
   ::const PRS &
   -- ^prsg
  RandomizedLDS
   ->
   ::const LDS &
   -- ^ldsg
  RandomizedLDS
   ->
   ::Size
   -- ^dimensionality
   ::BigNatural
   =0
   -- ^ldsSeed
   ::BigNatural
   =0
   -- ^prsSeed
  -- |returns next sample using a given randomizing vector
  nextSequence
   ->const sample_type &
  lastSequence
   ->const sample_type &
  -- |update the randomizing vector and re-initialize the low discrepancy generator
  nextRandomizer
   ->void
  dimension
   ->Size
