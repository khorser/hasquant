FiniteDifferenceModel
 -- |Generic finite difference model.
 -f-
  FiniteDifferenceModel
   ->
   ::const operator_type &
   -- ^L
   ::const bc_set &
   -- ^bcs
   ::const std::vector< Time > &
   =std::vector< Time >()
   -- ^stoppingTimes
  FiniteDifferenceModel
   ->
   ::const Evolver &
   -- ^evolver
   ::const std::vector< Time > &
   =std::vector< Time >()
   -- ^stoppingTimes
  evolver
   ->const Evolver &
  -- |solves the problem between the given times. Warningbeing this a rollback, from must be a later time than to.
  rollback
   ->void
   ::array_type &
   -- ^a
   ::Time
   -- ^from
   ::Time
   -- ^to
   ::Size
   -- ^steps
  -- |solves the problem between the given times, applying a condition at every step. Warningbeing this a rollback, from must be a later time than to.
  rollback
   ->void
   ::array_type &
   -- ^a
   ::Time
   -- ^from
   ::Time
   -- ^to
   ::Size
   -- ^steps
   ::const condition_type &
   -- ^condition
