SwaptionHelper
 -- |calibration helper for ATM swaption
 -- |BugThis helper does not register with the passed IBOR index and with the evaluation date. Furthermore, the ATM exercise rate is not recalculated when any of its observables change.
 -f-
  SwaptionHelper
   ->
   ::const Period &
   -- ^maturity
   ::const Period &
   -- ^length
   ::const Handle< Quote > &
   -- ^volatility
   ::const boost::shared_ptr< IborIndex > &
   -- ^index
   ::const Period &
   -- ^fixedLegTenor
   ::const DayCounter &
   -- ^fixedLegDayCounter
   ::const DayCounter &
   -- ^floatingLegDayCounter
   ::const Handle< YieldTermStructure > &
   -- ^termStructure
   ::CalibrationHelper::CalibrationErrorType
   =CalibrationHelper::RelativePriceError
   -- ^errorType
