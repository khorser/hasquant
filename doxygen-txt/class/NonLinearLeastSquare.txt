NonLinearLeastSquare
 -- |Non-linear least-square method.
 -- |Using a given optimization algorithm (default is conjugate gradient),\[ min \{ r(x) : x in R^n \} \] where \form#229 is the Euclidean norm of \form#230@_fakenl for some vector-valued function \form#37 from $ R^n $ to $ R^m $, \[ f = (f_1, ..., f_m) \] with $ f_i(x) = b_i - \phi(x,t_i) $ where $ b $ is the vector of target data and $ phi $ is a scalar function.Assuming the differentiability of $ f $, the gradient of $ r $ is defined by \[ grad r(x) = f'(x)^t.f(x) \]

 -f-
  -- Default constructor.
  NonLinearLeastSquare
   ->
   ::Constraint &
   -- ^c
   ::Real
   =1e-4
   -- ^accuracy
   ::Size
   =100
   -- ^maxiter
  -- Default constructor.
  NonLinearLeastSquare
   ->
   ::Constraint &
   -- ^c
   ::Real
   -- ^accuracy
   ::Size
   -- ^maxiter
   ::boost::shared_ptr< OptimizationMethod >
   -- ^om
  -- Destructor.
  ~NonLinearLeastSquare
   ->
  -- Solve least square problem using numerix solver.
  perform
   ->Array &
   ::LeastSquareProblem &
   -- ^lsProblem
  setInitialValue
   ->void
   ::const Array &
   -- ^initialValue
  -- return the results
  results
   ->Array &
  -- return the least square residual norm
  residualNorm
   ->Real
  -- return last function value
  lastValue
   ->Real
  -- return exit flag
  exitFlag
   ->Integer
  -- return the performed number of iterations
  iterationsNumber
   ->Integer
 -v-
 -e-