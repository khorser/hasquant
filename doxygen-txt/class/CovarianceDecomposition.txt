CovarianceDecomposition
 -- |Covariance decomposition into correlation and variances.
 -- |Extracts the correlation matrix and the vector of variances out of the input covariance matrix.Note that only the lower symmetric part of the covariance matrix is used.The covariance matrix must be symmetric. Testscross checked with getCovariance

 -f-
  -- |covarianceMatrix must be symmetric
  CovarianceDecomposition
   ->
   ::const Matrix &
   -- ^covarianceMatrix
   ::Real
   =1.0e-12
   -- ^tolerance
  -- |returns the variances Array
  variances
   ->const Array &
  -- |returns the standard deviations Array
  standardDeviations
   ->const Array &
  -- |returns the correlation matrix
  correlationMatrix
   ->const Matrix &