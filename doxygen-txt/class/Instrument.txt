Instrument
 -- |Abstract instrument class.
 -- |This class is purely abstract and defines the interface of concrete instruments which will be derived from this one.Testsobservability of class instances is checked.
 -f-
  -- |returns the error estimate on the NPV when available.
  errorEstimate
   ->Real
  -- |returns the date the net present value refers to.
  valuationDate
   ->const Date &
  -- |returns any additional result returned by the pricing engine.
  result
   ->T
   ::const std::string &
   -- ^tag
  -- |returns all additional result returned by the pricing engine.
  additionalResults
   ->const std::map< std::string, boost::any > &
  -- |returns whether the instrument might have value greater than zero.
  isExpired
   ->bool
 -f-
  -- |When a derived argument structure is defined for an instrument, this method should be overridden to fill it. This is mandatory in case a pricing engine is used.
  setupArguments
   ->void
   ::PricingEngine::arguments *
   -- ^
  -- |When a derived result structure is defined for an instrument, this method should be overridden to read from it. This is mandatory in case a pricing engine is used.
  fetchResults
   ->void
   ::const PricingEngine::results *
   -- ^
