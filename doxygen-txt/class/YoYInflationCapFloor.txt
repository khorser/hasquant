YoYInflationCapFloor
 -- |Base class for yoy inflation cap-like instruments.
 -- |Note that the standard YoY inflation cap/floor defined here is different from nominal, because in nominal world standard cap/floors do not have the first optionlet. This is because they set in advance so there is no point. However, yoy inflation generally sets (effectively) in arrears, (actually in arrears vs lag of a few months) thus the first optionlet is relevant. Hence we can do a parity test without a special definition of the YoY cap/floor instrument. Tests the relationship between the values of caps, floors and the resulting collars is checked.the put-call parity between the values of caps, floors and swaps is checked.the correctness of the returned value is tested by checking it against a known good value.

 -f-
  type
   ->Type
  capRates
   ->const std::vector< Rate > &
  floorRates
   ->const std::vector< Rate > &
  yoyLeg
   ->const Leg &
  startDate
   ->Date
  maturityDate
   ->Date
  lastYoYInflationCoupon
   ->boost::shared_ptr< YoYInflationCoupon >
  -- Returns the n-th optionlet as a cap/floor with only one cash flow.
  optionlet
   ->boost::shared_ptr< YoYInflationCapFloor >
   ::const Size
   -- ^n
 -v-
 -e-
 -f-
 -v-
 -e-
  Type
   Cap
   Floor
   Collar
 -f-
  YoYInflationCapFloor
   ->
   ::Type
   -- ^type
   ::const Leg &
   -- ^yoyLeg
   ::const std::vector< Rate > &
   -- ^capRates
   ::const std::vector< Rate > &
   -- ^floorRates
  YoYInflationCapFloor
   ->
   ::Type
   -- ^type
   ::const Leg &
   -- ^yoyLeg
   ::const std::vector< Rate > &
   -- ^strikes
  atmRate
   ->Rate
   ::const YieldTermStructure &
   -- ^discountCurve
  -- implied term volatility
  impliedVolatility
   ->Volatility
   ::Real
   -- ^price
   ::const Handle< YoYInflationTermStructure > &
   -- ^yoyCurve
   ::Volatility
   -- ^guess
   ::Real
   =1.0e-4
   -- ^accuracy
   ::Natural
   =100
   -- ^maxEvaluations
   ::Volatility
   =1.0e-7
   -- ^minVol
   ::Volatility
   =4.0
   -- ^maxVol
 -v-
 -e-