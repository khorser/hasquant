GenericRiskStatistics
 -- |empirical-distribution risk measures
 -- |This class wraps a somewhat generic statistic tool and adds a number of risk measures (e.g.: value-at-risk, expected shortfall, etc.) based on the data distribution as reported by the underlying statistic tool.Possible enhancementsadd historical annualized volatility
 -f-
  -- |returns the variance of observations below the mean, \[ \frac{N}{N-1} \mathrm{E}\left[ (x-\langle x \rangle)^2 \;|\; x < \langle x \rangle \right]. \] See Markowitz (1959).
  semiVariance
   ->Real
  -- |returns the semi deviation, defined as the square root of the semi variance.
  semiDeviation
   ->Real
  -- |returns the variance of observations below 0.0, \[ \frac{N}{N-1} \mathrm{E}\left[ x^2 \;|\; x < 0\right]. \]
  downsideVariance
   ->Real
  -- |returns the downside deviation, defined as the square root of the downside variance.
  downsideDeviation
   ->Real
  -- |returns the variance of observations below target, \[ \frac{N}{N-1} \mathrm{E}\left[ (x-t)^2 \;|\; x < t \right]. \] See Dembo and Freeman, "The Rules Of Risk", Wiley (2001).
  regret
   ->Real
   ::Real
   -- ^target
  -- |potential upside (the reciprocal of VAR) at a given percentile
  -- |percentile must be in range [90%-100%)
  potentialUpside
   ->Real
   ::Real
   -- ^percentile
  -- |value-at-risk at a given percentile
  -- |percentile must be in range [90%-100%)
  valueAtRisk
   ->Real
   ::Real
   -- ^percentile
  -- |expected shortfall at a given percentile
  -- |returns the expected loss in case that the loss exceeded a VaR threshold,\[ \mathrm{E}\left[ x \;|\; x < \mathrm{VaR}(p) \right], \] that is the average of observations below the given percentile \form#34. Also know as conditional value-at-risk. See Artzner, Delbaen, Eber and Heath, "Coherent measures of risk", Mathematical Finance 9 (1999)percentile must be in range [90%-100%)
  expectedShortfall
   ->Real
   ::Real
   -- ^percentile
  -- |probability of missing the given target, defined as \[ \mathrm{E}\left[ \Theta \;|\; (-\infty,\infty) \right] \] where \[ \Theta(x) = \left\{ \begin{array}{ll} 1 & x < t \\ 0 & x \geq t \end{array} \right. \]
  shortfall
   ->Real
   ::Real
   -- ^target
  -- |averaged shortfallness, defined as \[ \mathrm{E}\left[ t-x \;|\; x<t \right] \]
  averageShortfall
   ->Real
   ::Real
   -- ^target
