Bond
 -- |Base bond class.
 -- |Derived classes must fill the uninitialized data members.WarningMost methods assume that the cash flows are stored sorted by date, the redemption(s) being after any cash flow at the same date. In particular, if there's one single redemption, it must be the last cash flow,Tests price/yield calculations are cross-checked for consistency.price/yield calculations are checked against known good values.

 -f-
  settlementDays
   ->Natural
  calendar
   ->const Calendar &
  notionals
   ->const std::vector< Real > &
  notional
   ->Real
   ::Date
   =Date()
   -- ^d
  -- returns all the cashflows, including the redemptions.
  cashflows
   ->const Leg &
  -- returns just the redemption flows (not interest payments)
  redemptions
   ->const Leg &
  -- returns the redemption, if only one is defined
  redemption
   ->const boost::shared_ptr< CashFlow > &
  startDate
   ->Date
  maturityDate
   ->Date
  issueDate
   ->Date
  isTradable
   ->bool
   ::Date
   =Date()
   -- ^d
  settlementDate
   ->Date
   ::Date
   =Date()
   -- ^d
 -v-
 -e-
 -f-
  -- theoretical clean price
  -- The default bond settlement is used for calculation.Warningthe theoretical price calculated from a flat term structure might differ slightly from the price calculated from the corresponding yield by means of the other overload of this function. If the price from a constant yield is desired, it is advisable to use such other overload.
  cleanPrice
   ->Real
  -- theoretical dirty price
  -- The default bond settlement is used for calculation.Warningthe theoretical price calculated from a flat term structure might differ slightly from the price calculated from the corresponding yield by means of the other overload of this function. If the price from a constant yield is desired, it is advisable to use such other overload.
  dirtyPrice
   ->Real
  -- theoretical settlement value
  -- The default bond settlement date is used for calculation.
  settlementValue
   ->Real
  -- theoretical bond yield
  -- The default bond settlement and theoretical price are used for calculation.
  yield
   ->Rate
   ::const DayCounter &
   -- ^dc
   ::Compounding
   -- ^comp
   ::Frequency
   -- ^freq
   ::Real
   =1.0e-8
   -- ^accuracy
   ::Size
   =100
   -- ^maxEvaluations
  -- clean price given a yield and settlement date
  -- The default bond settlement is used if no date is given.
  cleanPrice
   ->Real
   ::Rate
   -- ^yield
   ::const DayCounter &
   -- ^dc
   ::Compounding
   -- ^comp
   ::Frequency
   -- ^freq
   ::Date
   =Date()
   -- ^settlementDate
  -- dirty price given a yield and settlement date
  -- The default bond settlement is used if no date is given.
  dirtyPrice
   ->Real
   ::Rate
   -- ^yield
   ::const DayCounter &
   -- ^dc
   ::Compounding
   -- ^comp
   ::Frequency
   -- ^freq
   ::Date
   =Date()
   -- ^settlementDate
  -- settlement value as a function of the clean price
  -- The default bond settlement date is used for calculation.
  settlementValue
   ->Real
   ::Real
   -- ^cleanPrice
  -- yield given a (clean) price and settlement date
  -- The default bond settlement is used if no date is given.
  yield
   ->Rate
   ::Real
   -- ^cleanPrice
   ::const DayCounter &
   -- ^dc
   ::Compounding
   -- ^comp
   ::Frequency
   -- ^freq
   ::Date
   =Date()
   -- ^settlementDate
   ::Real
   =1.0e-8
   -- ^accuracy
   ::Size
   =100
   -- ^maxEvaluations
  -- accrued amount at a given date
  -- The default bond settlement is used if no date is given.
  accruedAmount
   ->Real
   ::Date
   =Date()
   -- ^d
 -v-
 -e-
 -f-
  -- constructor for amortizing or non-amortizing bonds.
  -- Redemptions and maturity are calculated from the coupon data, if available. Therefore, redemptions must not be included in the passed cash flows.
  Bond
   ->
   ::Natural
   -- ^settlementDays
   ::const Calendar &
   -- ^calendar
   ::const Date &
   =Date()
   -- ^issueDate
   ::const Leg &
   =Leg()
   -- ^coupons
  -- old constructor for non amortizing bonds.
  -- WarningThe last passed cash flow must be the bond redemption. No other cash flow can have a date later than the redemption date.
  Bond
   ->
   ::Natural
   -- ^settlementDays
   ::const Calendar &
   -- ^calendar
   ::Real
   -- ^faceAmount
   ::const Date &
   -- ^maturityDate
   ::const Date &
   =Date()
   -- ^issueDate
   ::const Leg &
   =Leg()
   -- ^cashflows
  -- Expected next coupon: depending on (the bond and) the given date the coupon can be historic, deterministic or expected in a stochastic sense. When the bond settlement date is used the coupon is the already-fixed not-yet-paid one.The current bond settlement is used if no date is given.
  nextCouponRate
   ->Rate
   ::Date
   =Date()
   -- ^d
  -- Previous coupon already paid at a given date.
  -- Expected previous coupon: depending on (the bond and) the given date the coupon can be historic, deterministic or expected in a stochastic sense. When the bond settlement date is used the coupon is the last paid one.The current bond settlement is used if no date is given.
  previousCouponRate
   ->Rate
   ::Date
   =Date()
   -- ^d
  nextCashFlowDate
   ->Date
   ::Date
   =Date()
   -- ^d
  previousCashFlowDate
   ->Date
   ::Date
   =Date()
   -- ^d
 -v-
 -e-